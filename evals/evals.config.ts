import { defineEvalConfig } from "eve/evals";

// Shared defaults for every eval. Add a `judge` model and `reporters`
// (e.g. Braintrust) here when LLM-as-judge assertions or reporting are needed.
export default defineEvalConfig({});
